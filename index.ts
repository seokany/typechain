const name = "이석환",
    age = 22,
    gender = "male";

const greet = (name, age, gender) => {
    console.log(`Hello ${name}, you are ${age}, you are a ${gender}`);
};

greet(name, age, gender);
export {};